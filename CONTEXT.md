# paopao — 领域词汇表

> 只收术语，不收实现细节。新增术语随会话实时沉淀。

- **代理模式（agent 模式）**：REPL 的常开行为。用户输入任务后，模型有机会调用工具再回答，而不是直接答一句就走。与「纯聊天」相对。
- **ReAct 循环**：代理模式下每轮输入的处理循环 —— 模型思考 → （可选）发工具调用请求 → 执行工具 → 结果喂回 → 再思考 … 直到模型给出最终回答。
- **工具判定回合**：循环中「模型决定是否调用工具」的那一次请求-响应。工具判定回合可能连续多轮（一次调用不够）。
- **工具调用（tool call）**：模型以原生 function-calling 发出的调用请求，含工具名与参数。
- **工具结果（tool result）**：执行工具后的返回，作为 `tool` 角色消息喂回模型继续推理。
- **最终回答**：模型不再调用工具、直接输出的正式回答。这是 ReAct 循环的出口，也是唯一流式输出的部分。
- **危险命令确认**：`run_command` 命中危险命令清单时，执行前向用户要一次 y/n。常规命令自动执行。
- **工具集（toolset）**：代理模式暴露给模型的一组工具。首发：get_time / run_command / read_file。

相关地图：wayfinding/map-react.md（进行中）。
